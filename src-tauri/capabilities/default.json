{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "SR-006 — the frontend gets the event bus, window controls and nothing else. Every privileged operation goes through a Rust command.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
